# WSL development environment (backend running in WSL)
# Prefer a dedicated app user with mysql_native_password.
NODE_ENV=development
PORT=4000

# If MySQL runs inside WSL, use localhost and your app user creds
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=tether_app
DB_PASSWORD=YourStrongP@ss
DB_NAME=tether
DB_CONN_LIMIT=10

# If MySQL runs on Windows host and localhost fails in your WSL version,
# set DB_HOST to your Windows host IP (e.g., check /etc/resolv.conf nameserver)
# DB_HOST=192.168.1.100

# Alternatively, if your MySQL is configured for socket access and your user matches,
# set DB_SOCKET to your socket path. Typically not needed for WSL when accessing Windows MySQL.
# DB_SOCKET=/var/run/mysqld/mysqld.sock

CORS_ORIGIN=*
