# Copy this file to .env and fill in values
NODE_ENV=development
PORT=4000

# MySQL/TiDB connection
# Option A: discrete fields
DB_HOST=gateway01.ap-southeast-1.prod.aws.tidbcloud.com
DB_PORT=4000
DB_USER=<username>
DB_PASSWORD=<password>
DB_NAME=tether
DB_CONN_LIMIT=10
# Enable TLS for TiDB Cloud
DB_SSL=true
# If you downloaded the TiDB CA, put it at certs/ca.pem
# DB_SSL_CA=certs/ca.pem

# Option B: URL form (not required by this app)
# DB_URL=mysql://<user>:<pass>@gateway01.ap-southeast-1.prod.aws.tidbcloud.com:4000/tether

# CORS
CORS_ORIGIN=*

# --- Cloudflare R2 / S3-compatible storage (optional) ---
# Name of your R2 bucket
S3_BUCKET=<bucket-name>
# Endpoint copied from R2 (S3 clients)
S3_ENDPOINT=https://<accountid>.r2.cloudflarestorage.com
# R2 S3 API credentials
S3_ACCESS_KEY_ID=<access-key-id>
S3_SECRET_ACCESS_KEY=<secret-access-key>
# R2 recommends region "auto"
S3_REGION=auto
# Only set if your endpoint requires path style; R2 typically works with virtual-hosted style
S3_FORCE_PATH_STYLE=false
# If your bucket is public or behind a CDN, set a public base URL to build object URLs
# Examples:
# S3_PUBLIC_BASE_URL=https://<bucket>.<accountid>.r2.cloudflarestorage.com
# S3_PUBLIC_BASE_URL=https://cdn.example.com
